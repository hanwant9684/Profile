services:
  - type: worker
    name: telegram-downloader-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: RUN_WEB_SERVER
        value: "True"
